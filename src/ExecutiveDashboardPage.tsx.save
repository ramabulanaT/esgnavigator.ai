

cat > vercel.json <<'JSON'
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
JSON



git remote -v
# If it’s NOT your repo, set it:
# git remote set-url origin git@github.com:ramabulanaT/esgnavigator.ai.git

# Ensure we’re on main
git branch -M main


